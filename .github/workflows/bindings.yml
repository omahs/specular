name: Bindings

on: [push, pull_request]


jobs:
  build:
    runs-on: ubuntu-latest
    steps:

      - name: checkout repo
        uses: actions/checkout@v3

      - uses: actions/setup-node@v3

      - name: Install dependencies 
        run: npm ci

      - name: Generate contracts Go bindings
        run: (cd ./clients/geth/specular && make)
 